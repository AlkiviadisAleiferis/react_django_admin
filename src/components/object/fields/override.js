/* --------------------------------

Create your field inputs and map them in
the mapping below, with the convention:

    `InputTypeField`: Field
    `TypeField`: Readonly Field

This file will be avoided to change
in order to provide easier merge-ability
and preservation to custom changes.

-------------------------------- */

export const fields_inputs_map_override = {};
export const fields_values_map_override = {};
