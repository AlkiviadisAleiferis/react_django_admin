/*
placeholder file.
Define your custom Components in this folder.

export function CustomViewContent(props) {
    const { custom_view_name } = useParams();
    const { loader_data } = useLoaderData();

    const CustomViewComponent = custom_views_map[custom_view_name];

    return (
        <>
            <CustomViewComponent loader_data={loader_data} custom_view_name={custom_view_name} />
        </>
    );
}
    
*/
