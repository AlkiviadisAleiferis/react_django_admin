export const ADMIN_SITE_PREFERENCES_OVERRIDE = {};

export const SITE_PATH_OVERRIDE = null;

export const TOKEN_EXP_OVERHEAD_OVERRIDE = null;

export const BASE_INFO_CACHE_TIME_OVERRIDE = null;

export const REACT_APP_DEFAULT_CACHE_TTL_OVERRIDE = null;

export const AUTHENTICATION_FIELD_NAME_OVERRIDE = null;

// an endpoint naming convention similarity is desired
// between the server's api and this app's routes
// to make the whole system more intuitive

export const API_ENDPOINTS_OVERRIDE = {};
